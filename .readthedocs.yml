version: 1.0
formats: []

build:
  os: "windows 11"
  tools:
    python: "3.8"

sphinx:
  configuration: docs/conf.py
  fail_on_warning: false
  builder: html

python:
  install:
    - method: pip
      path: .
      extra_requirements:
        - docs